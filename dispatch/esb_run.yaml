# ITOGO test server configuration
#database section
server:
 host: "postgres"
 database: esb_database
 user: postgres
 password: postgres
#service settings
# use default port 
#port: 8900  
logpath: /var/log/esb
dispatch: true
resend: true
deliver:
  sys:bp
#  sys:erp
